# Preprocess Scripts
This directory contains scripts for preprocessing data, so that it is prepared to be fed
into the training pipeline.

Namely, there is:
1. A script to aggregate the station data into one large dataframe.
2. A script to aggregate the NLCD data into one dataframe.
3. A script to aggregate the reference data to a dataframe.