# SVGP
This repository contains code to model urban heat with sparse variational Gaussian processes.

## Code organization

The script `main.py` contains the main training logic for the experiments. Models are defined in the `Models/` directory, dataloaders in the `Datasets/` directory, and scripts used to process large datasets are in the `scripts/` directory.
